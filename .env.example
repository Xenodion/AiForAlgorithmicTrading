# Copy this file to .env and fill in your values.
# .env is gitignored — NEVER commit real credentials.

# IBKR Client Portal Gateway connection settings (base URL, port, timeouts)
# live in configs/broker.yaml, not here. Login is done manually via browser
# at https://localhost:5000 — see docs/README.md.

# Log level for scripts/bootstrap.py: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO
